import { NavLink } from "react-router-dom";

const Navigation = () => {
    return (
        <>
            <nav >
                <NavLink to='/'>Reviews</NavLink>
            </nav>
        </>
    )
};

export default Navigation